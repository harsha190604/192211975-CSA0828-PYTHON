def calculate_average(numbers):
    
    if not numbers:
        return "List is empty"
    
    
    total = sum(numbers)
    
    average = total / len(numbers)
    return average


num_list = [float(x) for x in input("Enter numbers separated by space: ").split()]
print("Average of the numbers:", calculate_average(num_list))
