def square_root(number, epsilon=0.0001):
  guess = number / 2.0
  while abs(guess**2 - number) >= episilon:
    guess = guess - ((guess **2 - number) / (2 * guess))
    
   return guess
number = float(input("Enter a  number:"))
sqrt = square_root(number)
