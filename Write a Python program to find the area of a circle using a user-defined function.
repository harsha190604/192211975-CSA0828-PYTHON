import math

def calculate_area_of_circle(radius):
    area = math.pi * radius**2
    return area


user_radius = float(input("Enter the radius of the circle: "))
circle_area = calculate_area_of_circle(user_radius)
print(f"Area of the circle: {circle_area:.2f}")
